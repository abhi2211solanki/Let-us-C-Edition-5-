#include <stdio.h>  // Include the stdio.h header for printf

int main() {        // Explicitly declare the return type as int
    printf("this is just a test ");
    return 0;       // Return 0 to indicate successful execution
}
